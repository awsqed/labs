# Ubuntu Server Initialization Configuration
# Copy this file to .env.init and customize the values below

# Username for the new non-root user (required)
NEW_USER="YOUR_USERNAME"

# SSH port - use 22 or choose 1024-65535 for non-standard port
SSH_PORT="22"

# SSH public key for the new user (required)
# Example: ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAABAQC... user@host
SSH_PUBLIC_KEY="YOUR_SSH_PUBLIC_KEY_HERE"

# Password for the new user (optional)
# Leave empty to be prompted during script execution
# Can also be set via environment variable: sudo USER_PASSWORD='secret' ./init.sh
USER_PASSWORD=""

# Hostname for the Postfix mail server
# Update this to your server's hostname or FQDN
MAIL_HOSTNAME="mail.example.com"
